@page
@model RegistrationOfPasses.Pages.SubjectTeachers.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>SubjectTeacher</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SubjectTeacher.IdSubject" class="control-label"></label>
                <select asp-for="SubjectTeacher.IdSubject" class ="form-control" asp-items="ViewBag.IdSubject"></select>
            </div>
            <div class="form-group">
                <label asp-for="SubjectTeacher.CnG" class="control-label"></label>
                <select asp-for="SubjectTeacher.CnG" class ="form-control" asp-items="ViewBag.CnG"></select>
            </div>
            <div class="form-group">
                <label asp-for="SubjectTeacher.CnT" class="control-label"></label>
                <select asp-for="SubjectTeacher.CnT" class ="form-control" asp-items="ViewBag.CnT"></select>
            </div>
            <div class="form-group">
                <label asp-for="SubjectTeacher.ConsultHourCount" class="control-label"></label>
                <input asp-for="SubjectTeacher.ConsultHourCount" class="form-control" />
                <span asp-validation-for="SubjectTeacher.ConsultHourCount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SubjectTeacher.AddControlHourCount" class="control-label"></label>
                <input asp-for="SubjectTeacher.AddControlHourCount" class="form-control" />
                <span asp-validation-for="SubjectTeacher.AddControlHourCount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
